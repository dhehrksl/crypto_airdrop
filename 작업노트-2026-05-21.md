# 작업 노트 — 2026-05-21

오늘 한 작업과 내일 이어서 할 일 정리. (커밋은 아직 안 함)

---

## 오늘 한 일

### 1. 스크래퍼가 4일간 안 돌았던 문제
- 원인: 스크래퍼 cron은 **백엔드 서버가 켜져 있는 동안 매시 정각에만** 실행됨. 부팅 즉시 1회 실행은 안 함. 5/17~5/21 서버를 안 띄워놔서 데이터가 묵음.
- 조치: `npm run scraper:once` 수동 실행 → RSS 363건 수집, 신규 에어드랍 7건 저장.

### 2. `trust_score` → `trend_score` 마이그레이션 누락 버그 (중요)
- 커밋 `f689001`에서 필드명을 `trust_score` → `trend_score`로 바꿨으나 **기존 DB 문서를 마이그레이션 안 함**.
- API 필터(`airdropController.getAirdrops`)가 `trend_score >= 20`을 요구 → 옛 필드명만 가진 문서 19개가 앱에서 전부 사라져 있었음. (마감일과 무관)
- 그 19개는 전부 `curated` + Telegram 출처 = 실제 알맹이 있는 에어드랍이었음.
- 조치: **로컬 DB 마이그레이션 완료** (19건 변환).
- 새 스크립트: `backend/scripts/migrations/rename-trust-score-to-trend-score.js`
  - 사용법: `node scripts/migrations/rename-trust-score-to-trend-score.js --apply`

### 3. RSS 분류 강화 (코드 수정 — 미커밋)
- `backend/src/services/scraper.js`:
  - `RETROSPECTIVE_TITLE_REGEX`, `POST_EVENT_TITLE_REGEX` 추가 — "ETHFI Value Drops 35%" 같은 회고성 가격분석 기사를 잡음.
  - `shouldDemoteAirdrop`에 제목 기반 강등 로직 추가.
  - AI 프롬프트의 `is_airdrop` 규칙 강화 — false 케이스(가격분석/추측성/리스트형/일반 뉴스) 명시.
- 기존 잘못 저장된 가격분석 기사 2건(Ether.fi, Wormhole) → `is_airdrop:false` 강등 완료.
- ※ 이 가드 변경은 **앞으로의 스크랩에만** 적용됨. 스크래퍼는 기존 문서를 재분류하지 않음.

### 4. 텔레그램 Draft 수집
- 새 스크립트: `backend/scripts/draft/collect.js` (Reddit+Telegram 수동 수집 트리거)
- 텔레그램은 빠진 게 아니라 이미 핵심 소스로 동작 중 — 메인 스크래퍼가 아니라 **Draft 수집기 → 관리자 검수 큐**로 들어감 (품질 관리 목적).
- 1차 실행: Gemini 503(과부하)으로 AI 추출 실패. 재실행 성공.
- 결과: 18개 채널 → 19건 수집 → 검수 큐 12건 적재.

### 5. 검수 큐 12건 정리
- **거절 7건**: Roam(앱 업데이트 안내), Arc Testnet(잡담 노이즈), WaterX(만료), Onyx·TeQoin·Harbor ML·Nimiq(트위터팔로우+봇제출형 농장 패턴).
- **승인 5건**: T-Rex Chain, YOM TGE, Cuties NFT, Lunascape, FishPeg.

### 현재 DB 상태
- 앱 노출 Airdrop: **30개** (세션 시작 시 7개 → 30개)
- pending draft: 0건

---

## 내일 이어서 할 일

- [ ] **운영 Atlas DB에 `trust_score→trend_score` 마이그레이션 실행**
      `MONGODB_URI="<Atlas 주소>" node scripts/migrations/rename-trust-score-to-trend-score.js --apply`
      (운영 DB에도 같은 버그가 있을 것)
- [ ] 미커밋 변경 커밋 — 이번 세션 작업 + 기존 출시 준비 작업(트래킹/워치리스트/마감 리마인더, 프론트 화면, 정책 문서 등) 약 40개 파일. 주제별로 나눌지 결정.
- [ ] 승인된 **YOM·Cuties** draft의 `official_link`가 텔레그램 게시글 링크(`t.me/...`)임 → 관리자 화면에서 실제 프로젝트 URL로 수정.
      (Cuties 실 URL: `cutiesnft.xyz/nftpass`)
- [ ] **EigenLayer 중복 2건** 중 하나 삭제 (서로 다른 RSS 기사라 `unique_hash`가 달라 둘 다 저장됨).
- [ ] 강화된 RSS 분류 로직이 다음 스크랩에서 의도대로 동작하는지 확인.
- [ ] 운영 환경(Render)에서 백엔드 서버가 계속 떠 있어 스크래퍼 cron이 매시 자동 실행되는지 확인.

---

## 이번 세션 미커밋 코드 변경

| 파일 | 상태 | 내용 |
|---|---|---|
| `backend/src/services/scraper.js` | 수정 | 분류 강화 (※ 이전 출시 작업 변경도 섞여 있음) |
| `backend/scripts/migrations/rename-trust-score-to-trend-score.js` | 신규 | trust_score→trend_score 마이그레이션 |
| `backend/scripts/draft/collect.js` | 신규 | Draft(Reddit+Telegram) 수동 수집 스크립트 |

> 그 외 백엔드/프론트엔드/문서/openspec 전반의 미커밋 변경(출시 준비 작업)은 이번 세션과 무관하게 이전부터 있던 것.
